// import { ResourceRef, Schema } from "../types";
// import {I} from "ajv";

// export const validWith = (validator, data, continueFn) => {
//   if (validator(data)) {
//     return va
//   } else {

//   }
// }
